# BACKLOG — LeaveEasy (leaveeasy-oneshot)

สิ่งที่ยังไม่เสร็จ หรือรู้ตัวว่าควรทำต่อ แต่ตั้งใจไม่ทำในรอบสั่งทีเดียวจบนี้ (ตามกติกาห้ามทำงานนอกสเปคของสัปดาห์นี้)

## 🔴 ความปลอดภัย — ต้องทำก่อนใช้งานจริง (ไม่ใช่แค่ในคาบเรียน)

**ย้ายคีย์ OpenRouter ออกจากฝั่งเบราว์เซอร์ไปไว้หลังเซิร์ฟเวอร์ที่ผู้ใช้แตะไม่ได้**
ตอนนี้ `js/ai-config.js` ฝังคีย์ OpenRouter ไว้ในโค้ดฝั่ง client ตรงๆ (ตามที่สเปคกำหนดว่าห้ามเขียนเซิร์ฟเวอร์เองใน Module 2) ใครก็เปิด DevTools ดูคีย์นี้ได้ และเรียกใช้แทนเราได้โดยไม่ต้องผ่านแอปเลย
เหมาะสำหรับโปรเจกต์เรียน/เดโมเท่านั้น **ก่อนใช้งานจริงกับผู้ใช้จำนวนมาก ต้องมี backend เล็กๆ (เช่น Cloud Function) เป็นตัวกลางเรียก OpenRouter แทน** แล้วให้เว็บเรียกมาที่ backend นั้นแทนการเรียก OpenRouter ตรง — เพื่อไม่ให้ผู้ใช้แตะคีย์ได้เลย

## 🟡 ฟีเจอร์ที่สเปคพูดถึงแต่ไม่มีรายละเอียดพอจะสร้าง

**"สรุปใบลาให้หัวหน้าอ่านก่อนกดอนุมัติ"** — ถูกพูดถึงในหัวข้อ 8 (แผนการเติบโตรายสัปดาห์ สัปดาห์ที่ 8) ว่าปุ่มผู้ช่วย AI ต้องทำสองอย่างคือ "จัดประเภทการลาจากเหตุผล + สรุปใบลาให้หัวหน้าอ่านก่อนกดอนุมัติ" แต่ไม่มี User Story, ไม่มี acceptance criteria, ไม่มีตำแหน่งในหน้าจอไหนเลยที่ระบุไว้ในหัวข้อ 3-4 ของสเปค
→ รอบนี้สร้างเฉพาะปุ่มจัดประเภทการลา (US-09) เท่านั้น ตามกติกาหัวข้อ 9 ที่บอกว่าเจอจุดแบบนี้ให้เสนอไว้แล้วรอคำสั่งก่อน
→ ถ้าต้องการฟีเจอร์นี้จริง ต้องเขียน User Story + acceptance criteria เพิ่มใน spec ก่อน (เช่น สรุปตรงไหนของหน้ารายละเอียด แสดงข้อมูลอะไรบ้าง ต้องกดยืนยันก่อนไหม)

## 🟢 อยู่นอกขอบเขต Module 2 ตามสเปคเอง (หัวข้อ 8-9) — ไม่ใช่บั๊ก แค่ยังไม่ถึงคิว

- **US-10** ค้นหา/กรอง/เรียงลำดับในหน้ารายการใบลา — สเปคกำหนดเป็น "งานต่อยอด" หรือ Backlog Sprint 2
- **US-11** แดชบอร์ดข้อมูลจริง (ตอนนี้ `dashboard.html` เป็นโครง prototype ตัวเลข 0 คงที่) — เป็นเนื้อหา Module 3
- **US-12** แนบเอกสารประกอบใบลา (ต้องใช้ Firebase Storage ซึ่งไม่อยู่ใน Module 2) — เป็นเนื้อหา Module 3

## 🔵 ปรับปรุงเพิ่มเติมที่ควรทำถ้ามีเวลา (ไม่กระทบสเปค)

- ชุดทดสอบ Playwright ตอนนี้ครอบคลุม 5 สถานการณ์หลักตามที่ใบงานสัปดาห์ 9 กำหนด (ยื่นใบลา/อนุมัติ/กรอกไม่ครบ/ความปลอดภัย 2 ตัว) ยังไม่ครอบคลุม US-05 (เขียนความเห็น), US-06 (จัดการประเภทการลาโดย hr), US-07 (ลบใบลา) แบบอัตโนมัติ — ทดสอบด้วยมือผ่านหมดแล้วระหว่างพัฒนา แต่ยังไม่มี Playwright spec แยกให้
- ยังไม่มีบัญชีทดสอบบทบาท `hr` แยกต่างหาก (ทดสอบสิทธิ์ hr เฉพาะจัดการประเภทการลาด้วยโค้ด/logic เท่านั้น ยังไม่เคยล็อกอินจริงเป็น hr)
