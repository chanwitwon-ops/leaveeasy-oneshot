// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — ต้นแบบการตั้งค่า AI (OpenRouter)
// ใช้กับปุ่ม "ให้ AI ช่วยจัดประเภทการลา" ในหน้ายื่นใบลาใหม่ (US-09)
//
// ไฟล์นี้ไม่มีคีย์จริง จึง commit ขึ้น GitHub ได้อย่างปลอดภัย
// ไฟล์จริงคือ js/ai-config.js ซึ่งถูกกันไว้ใน .gitignore แล้ว
//
// ═══════════════ วิธีตั้งค่า (ทำครั้งเดียว) ═══════════════
// 1. คัดลอกไฟล์นี้ในโฟลเดอร์ js/ แล้วเปลี่ยนชื่อสำเนาเป็น "ai-config.js"
//
// 2. สมัคร/ล็อกอิน OpenRouter → https://openrouter.ai/
//    → ไปที่ https://openrouter.ai/keys → กด "Create Key"
//    → คัดลอกคีย์ที่ได้ (ขึ้นต้นด้วย sk-or-v1-...)
//
// 3. วางคีย์แทนที่ค่า OPENROUTER_API_KEY ด้านล่างนี้
//
// 4. บันทึกไฟล์ js/ai-config.js แล้วเปิดหน้า new-leave-request.html ใหม่
//    ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" จะเรียก AI ได้ทันที
//    (ถ้ายังไม่สร้างไฟล์นี้ ปุ่มจะยังกดได้ แต่จะขึ้นข้อความเตือนว่ายังไม่ได้ตั้งค่าคีย์)
//
// ⚠️ คำเตือนความปลอดภัย: โค้ดนี้เรียก OpenRouter ตรงจากฝั่งเบราว์เซอร์ (client-side fetch)
//    ไม่มีเซิร์ฟเวอร์กลาง จึงหมายความว่า "ใครก็เปิด DevTools ดูคีย์นี้ได้"
//    เหมาะสำหรับโปรเจกต์เรียน/เดโมเท่านั้น ห้ามใช้คีย์ที่ผูกกับวงเงินสูง
//    หรือใช้วิธีนี้กับระบบจริงที่มีผู้ใช้จำนวนมาก
// ══════════════════════════════════════════════════════════

// คีย์ API ของ OpenRouter (https://openrouter.ai/keys)
const OPENROUTER_API_KEY = "วางคีย์ OpenRouter ของคุณที่นี่";

// โมเดลที่ใช้จัดประเภทการลา
// เลือก "openai/gpt-4o-mini" เพราะงานนี้คือการจัดหมวดหมู่ข้อความสั้น ๆ
// (เลือก 1 ชื่อจากรายชื่อประเภทการลาที่มีอยู่จริง) ไม่ต้องใช้เหตุผลซับซ้อน
// โมเดลเล็ก/ราคาถูกก็แม่นยำพอ และตอบเร็วกว่าโมเดลใหญ่มาก จึงประหยัดค่าใช้จ่ายโดยไม่เสียคุณภาพงาน
const OPENROUTER_MODEL = "openai/gpt-4o-mini";
