# รายงานผลการทดสอบ — LeaveEasy (leaveeasy-oneshot)

รันด้วย Playwright จริง (`npx playwright test`) เปิดเบราว์เซอร์จริง ต่อ Firebase project `leaveeasy-oneshot` (Firestore + Auth จริง ไม่ใช่ mock)

- **รันล่าสุดเมื่อ:** 2026-09-20 (เวลาไทย)
- **ผลรวม:** 10/10 ผ่าน — ใช้เวลารวมประมาณ 15 วินาที
- **คำสั่งรัน:** `npm test` (เทียบเท่า `npx playwright test`)
- **ไฟล์เทสต์:** โฟลเดอร์ [`tests/`](tests/)

| # | เทสต์ | ทดสอบอะไร | ผล | เวลา |
|---|---|---|:---:|---:|
| 1 | [`01-submit-and-list.spec.js`](tests/01-submit-and-list.spec.js) | ยื่นใบลาใหม่ (US-02) แล้วต้องเห็นในรายการใบลา (US-01) พร้อมชื่อผู้ขอลา/ประเภท/สถานะถูกต้อง และข้อมูลอยู่ทนหลังรีเฟรชหน้า (อ่านจาก Firestore จริง ไม่ใช่ state ชั่วคราว) | ✅ ผ่าน | 4.9s |
| 2 | [`02-approve.spec.js`](tests/02-approve.spec.js) | ผู้อนุมัติกดอนุมัติใบลา (US-04) แล้วสถานะเปลี่ยนเป็น "อนุมัติ" จริงทั้งหน้าจอและฐานข้อมูล ปุ่มอนุมัติ/ไม่อนุมัติหายไปหลังเป็นสถานะปลายทาง และอยู่ทนหลังเปิดหน้าใหม่ | ✅ ผ่าน | 6.9s |
| 3 | [`03-validation.spec.js`](tests/03-validation.spec.js) | กรอกฟอร์มยื่นใบลาโดยเว้นช่อง "หัวข้อ" ไว้ แล้วกดบันทึก (เกณฑ์ยอมรับของ US-02) ต้องไม่บันทึกใบลาใหม่ลง Firestore และต้องขึ้นข้อความเตือน "กรอกไม่ครบ" | ✅ ผ่าน | 3.2s |
| 4a | [`04-security-no-auth.spec.js`](tests/04-security-no-auth.spec.js) (เทสต์ที่ 1) | 🔒 **ความปลอดภัย** — ไม่ล็อกอินแล้วเปิดหน้ารายการใบลา (US-08) ต้องถูกเด้งไปหน้า login ทันที และห้ามมีข้อมูลใบลาใดหลุดออกมาก่อนเด้ง | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 1.2s |
| 4b | [`04-security-no-auth.spec.js`](tests/04-security-no-auth.spec.js) (เทสต์ที่ 2) | 🔒 **ความปลอดภัย** — ไม่ล็อกอินแล้วพิมพ์ URL หน้ารายละเอียดใบลาตรงๆ ต้องเข้าไม่ได้เช่นกัน (กันการข้ามหน้ารายการเข้าตรงๆ) | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 0.5s |
| 5 | [`05-security-cross-account.spec.js`](tests/05-security-cross-account.spec.js) | 🔒 **ความปลอดภัย** — ล็อกอินด้วยบัญชีที่สอง (employee2) แล้วเปิดใบลาของบัญชีแรก (employee1) ตรงๆ ผ่าน URL ต้องเปิดไม่ได้ และห้ามมีเนื้อหาใบลาของอีกฝ่ายหลุดออกมาในหน้าจอเลย | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 6.6s |
| 6a | [`06-reject.spec.js`](tests/06-reject.spec.js) (เทสต์ที่ 1) | กฎหัวข้อ 6 ของสเปค: manager กด "ไม่อนุมัติ" **โดยไม่มีความเห็นเลย** ต้องถูกปฏิเสธ (`alert()` เตือนให้เขียนความเห็นก่อน) และสถานะต้องยังเป็น "รอพิจารณา" ทั้งบนจอและหลังรีเฟรช | ✅ ผ่าน | 8.2s |
| 6b | [`06-reject.spec.js`](tests/06-reject.spec.js) (เทสต์ที่ 2) | manager เขียนความเห็นก่อน แล้วกด "ไม่อนุมัติ" → สถานะเปลี่ยนเป็น "ไม่อนุมัติ" จริงทั้งจอและฐานข้อมูล ปุ่มอนุมัติ/ไม่อนุมัติหายไป (สถานะปลายทาง) และอยู่ทนหลังรีเฟรช | ✅ ผ่าน | 6.2s |
| 7a | [`07-leave-types-crud.spec.js`](tests/07-leave-types-crud.spec.js) (เทสต์ที่ 1) | US-06: hr เพิ่มประเภทการลาใหม่ → เห็นในตารางและในดรอปดาวน์ของหน้ายื่นใบลาทันที, แก้ไขชื่อ → เปลี่ยนจริงและอยู่ทนหลังรีเฟรช, ลบ → หายจากตารางจริงและอยู่ทนหลังรีเฟรช | ✅ ผ่าน | 6.5s |
| 7b | [`07-leave-types-crud.spec.js`](tests/07-leave-types-crud.spec.js) (เทสต์ที่ 2) | employee ธรรมดาเปิดหน้าประเภทการลา ต้องไม่เห็นฟอร์มเพิ่ม/ปุ่มแก้ไข-ลบ (US-06 + US-08 ซ่อน UI ตามบทบาท) | ✅ ผ่าน | 2.5s |

## ที่มาของเทสต์ 06 และ 07 (เพิ่มใหม่)

เพิ่มเพื่อปิด gap ที่พบจาก audit เทียบ `leaveeasy-spec.md`: ก่อนหน้านี้ไม่มีเทสต์คลุม reject-flow (US-04/05 กรณีไม่อนุมัติ) และ CRUD ประเภทการลา (US-06) เลย

**บัญชี hr:** ตอนเขียนเทสต์ 07 ยังไม่มีบัญชี `role: hr` ที่ล็อกอินได้จริงในระบบ (เอกสาร Firestore เดิมที่มี role hr เป็นข้อมูล seed ปลอม ไม่มี Firebase Auth user คู่กัน) จึงสมัครบัญชีใหม่ผ่าน `register.html` จริง (`hr@leaveeasy.com`, uid `K083s0ZOFsZ0rjvrsxoQdiwNEjk2`) แล้วให้เจ้าของโปรเจกต์ตั้งค่า `role: "hr"` ให้ผ่าน Firebase Console เอง (ผู้ใช้เปลี่ยน role ตัวเองไม่ได้โดยเจตนา ตาม `firestore.rules` — เป็นฟีเจอร์ความปลอดภัย ไม่ใช่บั๊ก) บัญชีนี้เพิ่มเข้า `tests/test-accounts.local.js` แล้ว (gitignore ไว้)

**สาเหตุที่เทสต์ 7a เคยไม่ผ่านตอนแรก (แก้แล้ว):** ไม่ใช่บั๊กของแอปและไม่ใช่ปัญหา role — เป็น **race condition ในตัวเทสต์เอง**: `js/leave-types.js` ผูก event listener ให้ปุ่ม "เพิ่มประเภทการลา" หลังจากรอ `window.currentUserPromise` (ต้องอ่าน Firestore `users/{uid}` ก่อนเพื่อรู้ว่าเป็น hr) แต่เทสต์เดิม fill+click ปุ่มทันทีหลัง `page.goto()` โดยไม่รอให้หน้าโหลดสิทธิ์เสร็จก่อน ทำให้บางครั้งคลิกไปตอนที่ listener ยังไม่ถูกผูก (คลิกไม่มีผลอะไรเลย ไม่มี error ให้เห็น) แก้โดยเพิ่มบรรทัดรอให้แถวเดิม ("ลาพักร้อน") render เสร็จก่อน ซึ่งเป็นสัญญาณว่า `currentUserPromise` resolve แล้วและปุ่มพร้อมใช้งาน — ยืนยันแล้วว่าหลังแก้ รันซ้ำหลายรอบผ่านสม่ำเสมอ 10/10

## หมายเหตุ

- ไม่มีการแก้โค้ดของระบบ (HTML/CSS/JS/`firestore.rules`) เพื่อให้เทสต์ผ่านเลย — การแก้ไขทั้งหมดที่ทำเพื่อให้ผ่านครบ 10/10 อยู่ในไฟล์เทสต์เท่านั้น (`tests/07-leave-types-crud.spec.js` เพิ่ม wait ก่อนอินเทอร์แอกชัน)
- ระหว่างพัฒนา (ก่อนเขียนเทสต์ชุดนี้) พบและแก้บั๊กจริง 1 จุดจากการทดสอบด้วยมือ: กฎเดิมปล่อยให้ผู้ใช้แก้ไขช่อง `role` ของตัวเองได้ (ยกระดับสิทธิ์เองได้) — แก้ใน `firestore.rules` แล้วก่อนเขียนเทสต์ชุดนี้
- บัญชีทดสอบที่ใช้เก็บไว้ใน `tests/test-accounts.local.js` (gitignore แล้ว ไม่ขึ้น GitHub) — ดูรูปแบบได้ที่ `tests/test-accounts.example.js` (เพิ่มรายการ `hr` เข้าไปในทั้งสองไฟล์แล้ว)

## Gap ที่ยังเหลือเทียบสเปค (ยังไม่มีเทสต์คลุม)

- US-07 ลบใบลาของตัวเองที่สถานะรอพิจารณา (`#delete-btn` ในหน้า detail)
- การสมัครสมาชิก/ล็อกอิน/ออกจากระบบ แบบเจาะจง (มีแค่ผ่าน helper กลาง)
- ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" (US-09) ทั้งกรณีสำเร็จ/timeout/ผลลัพธ์ผิดประเภท
- Firestore Security Rules ระดับกฎแยกต่างหาก (นอกเหนือจาก E2E ผ่านเบราว์เซอร์)
