# รายงานผลการทดสอบ — LeaveEasy (leaveeasy-oneshot)

รันด้วย Playwright จริง (`npx playwright test`) เปิดเบราว์เซอร์จริง ต่อ Firebase project `leaveeasy-oneshot` (Firestore + Auth จริง ไม่ใช่ mock)

- **รันล่าสุดเมื่อ:** 2026-09-20 10:57:41 น. (เวลาไทย)
- **ผลรวม:** 6/6 ผ่าน — ใช้เวลารวม 7.9 วินาที
- **คำสั่งรัน:** `npm test` (เทียบเท่า `npx playwright test`)
- **ไฟล์เทสต์:** โฟลเดอร์ [`tests/`](tests/)

| # | เทสต์ | ทดสอบอะไร | ผล | เวลา |
|---|---|---|:---:|---:|
| 1 | [`01-submit-and-list.spec.js`](tests/01-submit-and-list.spec.js) | ยื่นใบลาใหม่ (US-02) แล้วต้องเห็นในรายการใบลา (US-01) พร้อมชื่อผู้ขอลา/ประเภท/สถานะถูกต้อง และข้อมูลอยู่ทนหลังรีเฟรชหน้า (อ่านจาก Firestore จริง ไม่ใช่ state ชั่วคราว) | ✅ ผ่าน | 5.3s |
| 2 | [`02-approve.spec.js`](tests/02-approve.spec.js) | ผู้อนุมัติกดอนุมัติใบลา (US-04) แล้วสถานะเปลี่ยนเป็น "อนุมัติ" จริงทั้งหน้าจอและฐานข้อมูล ปุ่มอนุมัติ/ไม่อนุมัติหายไปหลังเป็นสถานะปลายทาง และอยู่ทนหลังเปิดหน้าใหม่ | ✅ ผ่าน | 6.8s |
| 3 | [`03-validation.spec.js`](tests/03-validation.spec.js) | กรอกฟอร์มยื่นใบลาโดยเว้นช่อง "หัวข้อ" ไว้ แล้วกดบันทึก (เกณฑ์ยอมรับของ US-02) ต้องไม่บันทึกใบลาใหม่ลง Firestore และต้องขึ้นข้อความเตือน "กรอกไม่ครบ" | ✅ ผ่าน | 3.2s |
| 4a | [`04-security-no-auth.spec.js`](tests/04-security-no-auth.spec.js) (เทสต์ที่ 1) | 🔒 **ความปลอดภัย** — ไม่ล็อกอินแล้วเปิดหน้ารายการใบลา (US-08) ต้องถูกเด้งไปหน้า login ทันที และห้ามมีข้อมูลใบลาใดหลุดออกมาก่อนเด้ง | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 1.1s |
| 4b | [`04-security-no-auth.spec.js`](tests/04-security-no-auth.spec.js) (เทสต์ที่ 2) | 🔒 **ความปลอดภัย** — ไม่ล็อกอินแล้วพิมพ์ URL หน้ารายละเอียดใบลาตรงๆ ต้องเข้าไม่ได้เช่นกัน (กันการข้ามหน้ารายการเข้าตรงๆ) | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 0.5s |
| 5 | [`05-security-cross-account.spec.js`](tests/05-security-cross-account.spec.js) | 🔒 **ความปลอดภัย** — ล็อกอินด้วยบัญชีที่สอง (employee2) แล้วเปิดใบลาของบัญชีแรก (employee1) ตรงๆ ผ่าน URL ต้องเปิดไม่ได้ และห้ามมีเนื้อหาใบลาของอีกฝ่ายหลุดออกมาในหน้าจอเลย | ✅ ผ่าน (เข้าไม่ได้ = ผ่าน) | 5.9s |

## ข้อที่ไม่ผ่าน

ไม่มี — รันล่าสุดผ่านทั้งหมด 6/6

## หมายเหตุ

- ไม่มีการแก้โค้ดของระบบ (HTML/CSS/JS/`firestore.rules`) เพื่อให้เทสต์ผ่านระหว่างเขียนชุดทดสอบนี้เลย
- ระหว่างพัฒนา (ก่อนเขียนเทสต์ชุดนี้) พบและแก้บั๊กจริง 1 จุดจากการทดสอบด้วยมือ: กฎเดิมปล่อยให้ผู้ใช้แก้ไขช่อง `role` ของตัวเองได้ (ยกระดับสิทธิ์เองได้) — แก้ใน `firestore.rules` แล้วก่อนเขียนเทสต์ชุดนี้
- บัญชีทดสอบที่ใช้เก็บไว้ใน `tests/test-accounts.local.js` (gitignore แล้ว ไม่ขึ้น GitHub) — ดูรูปแบบได้ที่ `tests/test-accounts.example.js`
